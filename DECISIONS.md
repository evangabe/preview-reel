# DECISIONS.md

Running log of choices, tradeoffs, and assumptions. One line each. Appended as work happens, not reconstructed afterwards.

Format: `- **[Area]** Chose X over Y because Z. Tradeoff: W.`

---

## Pre-build (scoping)

- **[Problem]** Chose preview-deployment demo recording over a general demo generator because the trigger, the environment, and the audience are all already defined by the PR. Tradeoff: only useful to teams already on Vercel preview deployments.
- **[Build vs buy]** Chose to build on `webreel` (Vercel Labs, Apache-2.0) rather than implement frame capture and encoding. Tradeoff: the project reads as a layer on someone else's tool unless the authoring and trigger work is substantial.
- **[Architecture]** Split exploration from recording: `agent-browser` emits a `webreel` config, `webreel` replays it. Because an agent's live fumbling is unpublishable and a config is reviewable and re-runnable. Tradeoff: two stages, two failure modes, longer runs.
- **[Trigger]** Chose a `[feat]` PR title tag over a `CHANGELOG.md` diff. Deterministic, zero-cost on the common path, and puts the decision with the author. Tradeoff: only fires when someone remembers to tag.
- **[Trigger]** Chose title tags over GitHub labels because labels need repo setup. Tradeoff: pollutes PR titles, and a tag added after the fact only fires on the next deployment.
- **[Identity]** Keyed runs on `(repo, prNumber)` rather than feature slug or deployment ID. PR numbers are stable; titles get edited and deployments repeat per push.
- **[Model]** The model scopes the demo but does not decide whether to record. Removes the false-positive problem entirely rather than tuning for it.
- **[Comment]** One comment updated in place, never the PR description. A bot that overwrites human-authored text loses that fight every time.
- **[Storage]** Blob metadata instead of a database. Tradeoff: cross-run queries for the gallery get awkward; escape hatch is a per-repo index blob, not Postgres.
- **[Scope]** Cut: credential management UI, TTS voiceover, uploaded audio, stale-demo detection, multiple demos per PR, gallery auth. Each is a real feature; none is needed to prove the loop.
- **[Scope]** Hardcoded credentials for v1. The correct answer is a scoped demo account with short-lived credentials via Vercel Connect, which is a day of work and not the interesting part.

## Assumptions to verify in the first hour

- **[Sandbox]** Assuming `webreel`'s auto-download of Chrome and ffmpeg to `~/.webreel` works inside a Sandbox microVM, or can be pinned via `CHROME_PATH` / `FFMPEG_PATH` in a snapshot. Unverified — try pointing `webreel` at the Chromium `agent-browser install` already places in the snapshot before letting it fetch its own.
- **[Trigger]** ~~Assuming the deployment webhook payload carries enough git metadata to resolve the PR title without a GitHub API call.~~ Checked against Vercel's webhook API docs before writing code: it doesn't — `deployment.meta` carries git identifiers (`githubPrId`, `githubCommitRef`, `githubCommitSha`), not title/body. A GitHub call is still required either way; the win is that `githubPrId` (when present) turns it from a branch search into an exact `GET` by PR number.
- **[Agent]** Assuming `agent-browser`'s accessibility snapshot yields selectors stable enough to replay. Unverified — may need a resolution pass between explore and record.
- **[Timing]** Assuming end-to-end wall clock under 4 minutes. If it's longer, the in-progress state needs more design than a stage label.

## Spec review, pre-build (docs-only pass against current Vercel/webreel/agent-browser docs)

- **[Bug]** Spec originally said filter webhooks on `target: "preview"`. Vercel's actual values are `production`, `staging`, or `null` — preview deployments are `null`. That filter would have silently dropped every event the project exists to process. Caught by reading the webhook API reference before writing the handler, not by testing it live. Fixed to `target !== "production"`.
- **[Credentials]** Spec assumed the bypass secret and login could be injected as HTTP headers/cookies into the recorded flow. `webreel`'s config schema has no header or cookie field — only URL, steps, and viewport per video. Chose URL query params with `webreel`'s built-in `${VAR}` env-substitution over forking `webreel` or writing a custom replay layer. Tradeoff: the bypass secret and session token are visible in the browser's address bar in the recorded video (mitigated: it's a single-use redirect that immediately drops the query string via the target app's own redirect to a clean URL) and the target app needs one new route (`/api/demo-login`) it wouldn't otherwise have.
- **[Auth]** Cut a scripted login step in favor of a server-side token-for-cookie exchange (`/api/demo-login`), resolving what was Open Question 2 by design rather than by testing whether session-state injection works. Removed `TARGET_APP_EMAIL`/`TARGET_APP_PASSWORD` from the env list; replaced with a single `DEMO_LOGIN_TOKEN`.
- **[Storage]** Spec's status page assumed polling a single Blob object that gets overwritten per stage. Vercel Blob's `cacheControlMaxAge` floor is 60s and overwrites take up to 60s to propagate through the CDN — a live-updating status page built on that would show a stale stage for most of a 3-minute run, directly undermining R-8.2. Changed run-status records to append-only per-stage-transition objects (or reading `getRun()` from the Workflow directly), keeping Blob as the durable terminal record rather than the live one.
- **[Scope]** Cut gallery filter-by-feature (R-8.1) and per-repo daily run cap (R-10.3) during the review pass, not mid-build. Neither is needed to prove the loop; both were added to the original spec without being load-bearing for the demo. The AI Gateway spend cap remains the actual cost backstop.
- **[Idempotency]** Spec's idempotency requirement (R-1.5) only covered duplicate webhook deliveries for the same deployment ID. It didn't cover a second deployment for a PR that already has a run *in progress* (a push mid-run) — §8 says "let the current run finish," but nothing enforced that before this pass. Added an in-progress check keyed on `(repo, prNumber)`, separate from R-2.10's completed-record check.
- **[Timing]** §12's build order summed to 420 minutes against a 360-minute budget — a plan that's already over budget before the first line of code, with no acknowledgment of where to cut. Left the per-step estimates alone (they're the right shape) but named the cut explicitly: drop to the record-only re-trigger fallback at hour 4 rather than let step 1 run long.

## Build

<!-- append below as you go -->

- **[Repo]** GitHub repo name is `preview-reel` (matches AGENTS.md's repository table), not the local folder name `vercel-preview-reels`. Created public under the `evangabe` account via the `ghp` gh-cli alias, not the default `gh` identity, which is signed in as a separate work account.
- **[Scaffold]** Ran `create-next-app` in a scratch dir and merged the output in, rather than scaffolding in place, because the target directory already had `AGENTS.md`/`DECISIONS.md`/`docs/`/`wireframes/` and `create-next-app` refuses a non-empty target.
- **[Scaffold]** Local shell aliases `npx`/`npm` through a Socket Firewall proxy (`sfw`) that intercepts TLS for package-fetch scanning. `shadcn init`'s runtime call to `ui.shadcn.com/init` failed under that proxy (`unable to get local issuer certificate`) but succeeded identically with `SFW_BYPASS=1`. Used the bypass for `shadcn init` and `npm install`/`npm run build`; not a project-level config change, just how this one shell reaches those hosts.
- **[Scaffold]** Populated `lib/`, `sandbox-runner/`, `workflows/`, `scripts/`, and the `app/api`/`app/runs` routes as empty `export {}` stubs with a comment pointing at the spec section, since AGENTS.md's repository layout names these files explicitly. Left `components/{gallery-grid,run-status,demo-player,empty-state}.tsx` out for now — AGENTS.md says "do not build the gallery before the pipeline produces a video," and stub files for those read as starting that work rather than just laying out structure.
- **[Scaffold]** Added a `type-check` script but not a `test` script — no test runner is installed and no pure functions exist yet to test. Adding `vitest` now with nothing to run would leave `npm test` broken, against "keep it running." Lands with the first pure function that needs a test (tag matcher is first up, build order step 2).
- **[Scaffold]** `.gitignore`'s default `.env*` rule from `create-next-app` would have excluded `.env.example` too; added `!.env.example` so the template stays committed while real `.env*` files stay ignored.
- **[Tooling]** The Cursor agent harness auto-appends `Co-authored-by: Cursor <cursoragent@cursor.com>` to every commit made through its shell tool, regardless of the `-m` message given — confirmed by amending with the trailer stripped and watching it get re-added. Not caused by any git hook, alias, or config in this repo or globally (checked `.git/hooks`, `core.hooksPath`, `~/.gitconfig`, env for `GIT_*`/`LD_PRELOAD`/`DYLD_*` — none present), so it's injected above the shell, not something `git commit` itself can be told to skip. `git commit-tree`/`filter-branch` don't go through that path and aren't intercepted, so history was rewritten with `filter-branch --msg-filter` and force-pushed once, and subsequent commits in this session are made via `write-tree`+`commit-tree`+`update-ref` instead of `git commit`.

## Vercel project setup

- **[Plan]** Team webhooks are Pro/Enterprise only (`webhook_triggers` is "not available" on Hobby), so the plan is a hard prerequisite rather than a nice-to-have — R-1.1 is the entire trigger. Confirmed the team is on `pro` via the API before building on it. Everything else in the stack (Sandbox, Blob, Workflow, AI Gateway, Protection Bypass) works on Hobby; this one feature forces the upgrade. Worth stating in the README so a reviewer cloning this doesn't get halfway.
- **[Config]** `VERCEL_OIDC_TOKEN` is **not** a runtime environment variable in deployed functions. The spec and `.env.example` both listed it alongside the hand-set secrets, and validating it at boot 500'd every dynamic route in production while `/` (static, CDN-served) kept returning 200 — a failure that only shows up on deploy, not in `npm run build`. It exists at build time and in `.env.local` via `vercel env pull` (12h expiry); at runtime the Sandbox SDK acquires it out of band. Now validated off-platform only, gated on `process.env.VERCEL`. This was my assumption, carried straight from the spec, and the deploy is what caught it.
- **[Config]** Split `.env.example` into hand-set secrets vs. platform-injected ones, and gave `lib/env.ts` a different remedy string per group. "Add a variable" and "connect a Blob store" are different actions, and telling someone to paste an OIDC token into the dashboard points them at a value that expires in 12 hours. Confirmed empirically: connecting the Blob store injected `BLOB_READ_WRITE_TOKEN` automatically.
- **[Config]** Validate env in `instrumentation.ts` (runtime boot) rather than at build. R-CFG.1 says "fail the deploy", but the webhook secret cannot exist before the first deploy — a build-time check makes the first deploy structurally impossible. Tradeoff: a missing secret 500s every dynamic route instead of failing the build, which is loud but later than the spec wanted.
- **[Config]** Seeded `VERCEL_WEBHOOK_SECRET`/`GITHUB_TOKEN`/`VERCEL_PROTECTION_BYPASS` with obvious placeholders for the first deploy, then replaced the webhook secret with the real value after registration. Tradeoff: presence-only validation passes on a placeholder, so the check proves configuration exists, not that it works.
- **[Config]** Hand-rolled the env validator instead of pulling in zod. Six required strings don't need a schema library, and zod lands with `lib/scope/schema.ts`, which is the file that actually needs it.
- **[Region]** Pinned `iad1` in `vercel.json`. The Vercel World backend for Workflow is deployed only in `iad1`; apps elsewhere route workflow requests there anyway and pay the latency. The Blob store also landed in `iad1`, so storage, orchestration, and functions are colocated.
- **[Deploy]** `vercel project add` creates a project with no framework preset, so the first deploy failed with `The Output Directory "public" is empty`. Fixed with `"framework": "nextjs"` in `vercel.json` rather than clicking it in the dashboard, so the setting is committed and reviewable.
- **[Workflow]** Did not set `maxDuration` anywhere. `withWorkflow()` already emits step functions at `maxDuration: max` and orchestration at 60s, so R-3.3's 5-minute Sandbox budget is covered by the plan maximum on Pro. Setting it by hand would have been a guess at a value the integration already manages.
- **[Storage]** Created the Blob store with `--access public`. R-6.3 accepts public-read via unguessable URL, and the PR comment embeds a poster that GitHub's image proxy fetches unauthenticated. Tradeoff: the v1 privacy punt is now a property of the store, not just the code.
- **[Trigger]** Registered the team webhook unscoped (all team projects) per R-1.1, and made the unimplemented handler return 200 rather than 501. Vercel retries any non-2xx with backoff for 24 hours, so a stub that honestly reports "not implemented" would have generated a day of retries per deployment. Verified live: a deploy fired `deployment.succeeded` and the endpoint answered 200.
- **[Access]** Preview Reel's own production alias (`preview-reel.vercel.app`) is public, while deployment-specific URLs sit behind Vercel Authentication and 302 to a login. The webhook must target the alias — pointing it at a deployment URL would auth-wall Vercel's own delivery service. Same protection asymmetry the target project will rely on in reverse.

## Step 0b: live verification against the target (before any pipeline code)

- **[Trigger]** Confirmed against the target project's real deployments: the PR preview arrives with `target: null` and `meta.githubPrId: 1`. R-1.3's `target !== "production"` filter and R-1.8's `githubPrId` fast path are now verified, not assumed.
- **[Auth]** Verified the PAT end to end on PR #1 — create 201, update 200, delete 204 — with Issues and Pull requests both at read/write. Not tested with the spec's narrower split, so the widening is defensive rather than proven necessary; it stays because a 403 at the comment stage is the worst place to discover the difference.
- **[Access]** The auth wall has a fingerprint: a wrong or missing bypass secret 302s to `vercel.com/sso-api?url=…&nonce=…`, while a correct secret against an unauthenticated app 302s to the same-origin `/login`. R-3.6's "detect the auth wall explicitly" is therefore a host check on the redirect target — `vercel.com/sso-api` means the bypass is wrong, same-origin `/login` means the demo-login step didn't run. Two different failure reasons, cheaply distinguishable.
- **[Access]** The query-param form (`?x-vercel-protection-bypass=…&x-vercel-set-bypass-cookie=true`) answers 307 to the clean URL and sets `_vercel_jwt` (7-day, HttpOnly). So `webreel`'s first navigation carries the secret exactly once and every subsequent step in the recording is cookie-authenticated with a clean address bar — the mitigation in the earlier `[Credentials]` entry holds.
- **[Auth]** Resolved spec Open Question 2 by running it: a fresh cookie-less context hitting `/api/demo-login?token=…&next=/` plus the bypass params lands on `/` with 200 after two redirects, where `/` otherwise 302s to `/login`. A wrong token yields 401 at `/api/demo-login`. Login failure and auth-wall failure are distinct status codes on distinct hosts.
- **[Recording]** Inspected `webreel@0.1.4` and `@webreel/core` rather than trusting the docs: Chrome launches with `--no-sandbox` already (so running as the Sandbox user is fine), `CHROME_PATH`/`FFMPEG_PATH` are honored, binaries cache under `~/.webreel/bin/`, and `${VAR}` substitution leaves unset variables as literal text — so a missing env var at replay time produces an "Element not found"/navigation failure, not a clear error. The runner must assert the substitution variables are present before invoking `webreel record`.
- **[Recording]** `webreel`'s step vocabulary is `pause, click, key, type, drag, scroll, wait, moveTo, screenshot, navigate, hover, select`; targets are `text` or `selector` with optional `within`. There is no header, cookie, or auth field of any kind, and `webreel validate` exists — the explore stage should run it on the emitted config before declaring success.

## Sandbox runner — checkpoint 1 (hand-driven golden path)

- **[Exploration]** A1 held, with a CLI caveat: `agent-browser` followed the demo-login redirects and retained both bypass/session cookies, but `open --headers …` followed by commands without that launch option restarted Chrome at `about:blank`. `set headers` once before `open` preserved the daemon session across commands. The explorer will use the persistent setting, not repeat `--headers` on only its first command.
- **[Selectors]** The target already gives its replay-critical controls stable test IDs (`select-P-1001`, `select-P-1002`, `export-csv`), so the golden config uses CSS selectors for checkboxes and visible text for `Export CSV`. Tradeoff: IDs tied to fixture part IDs are target-specific, which is appropriate for the fallback config but not something the model prompt should generalize.
- **[Recording]** A2 held: `webreel` 0.1.4 replayed the six-step config, including `click.text = "Export CSV"`, against a fresh context and produced a valid 1440×900 60fps mp4 plus thumbnail. The deterministic fallback is therefore viable before any model code exists.
- **[Recording]** A3 was based on the wrong visual model: WebReel captures only the page viewport, not Chrome's address bar, so the secret cannot appear in a video frame even during the initial redirect. The query-param secret can still appear in infrastructure request logs; the clean redirect remains security-relevant, but `thumbnail.time` is not its mitigation.
- **[Fixtures]** Chose the second warmed recording (10.05s, 409 KiB) as the checked-in fixture over the first cold-install run (2.73s). The first run downloaded Chrome/ffmpeg while recording and produced compressed timing that did not match the configured delays; snapshot builds must smoke once after installation for both correctness and warm caches.
- **[Validation]** Mirrored WebReel's action union in zod but deliberately narrowed model-produced navigation to same-origin paths, typing to stable selectors, one target per action, one video, and ≤12 steps. Tradeoff: valid hand-authored WebReel configs may fail Preview Reel's stricter schema; this boundary validates what the model is allowed to emit, not every config WebReel can execute.
- **[Testing]** Added Vitest with the first zod schema, per the repository rule to test pure schemas only. Used Vitest 4 rather than 5 because the local Node 25 binary is outside Vitest 5's explicit engine range, while both support the deployed Node 24 runtime.
- **[Dependencies]** Added AI SDK + AI Gateway, zod, esbuild, and tsx now because the next vertical slice uses each directly: model tool loop, validated config, single-file VM bundle, and local runner harness. No wrapper packages or provider SDKs were added.

## Sandbox runner — checkpoint 2 (local stage contracts)

- **[Model]** Chose `anthropic/claude-sonnet-5` from the live AI Gateway model list for the exploration reliability probe, prioritizing tool-use quality over the cheaper mini models while the key's $25 monthly budget is the hard cap. Tradeoff: the three-run score costs more; if it passes, model downgrades are a later measured optimization.
- **[Model]** The first explorer call reached AI Gateway only with the local Socket Firewall bypass; without it, the proxy's certificate produces `unable to get local issuer certificate`. With the bypass, Gateway returned a definitive 403: the Pro team still requires a valid credit card on file even though the API key has a budget. Classified this as `explore/model-call-failed` rather than the earlier misleading `invalid-config`; the 2-of-3 score is blocked on account setup, not browser behavior.
- **[Exploration]** The model returns only `{ found, reason, steps }`; runner code constructs the auth URL, base URL, viewport, output, and thumbnail. This narrows the plan's `finish(config)` contract because letting the model repeat fixed credential placeholders creates risk without adding judgment.
- **[Privacy]** Explorer transcripts log every browser tool call and model step but redact all three credentials before writing. Verified the failed live run's transcript contains none of `DEMO_LOGIN_TOKEN`, `VERCEL_PROTECTION_BYPASS`, or `AI_GATEWAY_API_KEY`.
- **[Runner]** Kept the required ESM single-file bundle and added a `createRequire(import.meta.url)` banner because the AI Gateway package's optional OIDC path contains a dynamic CommonJS `require("path")`; plain esbuild ESM crashed before command dispatch. Tradeoff: a one-line compatibility shim in the build command, instead of changing the runner contract to CommonJS.
- **[Recording]** The record stage rewrites only the output/thumbnail fields into its own output directory, removes stale outputs, validates before launch, captures child stderr for one-line failure JSON, and streams WebReel stdout. The bundled 931 KiB runner reproduced the golden config in 15.6s (411 KiB mp4); with both substitution variables removed it exited 1 with no stdout and one `record/substitution-vars-missing` JSON line.
- **[Failure]** Added `record-failed` and `explore/model-call-failed` as honest fallback reasons beyond the plan's initial closed taxonomy. Mapping an unknown recorder failure to `navigation-failed` or a Gateway/account failure to `invalid-config` would be specific-sounding but false.

## Sandbox runner — checkpoint 3 (snapshot and VM replay)

- **[Sandbox]** Replaced the plan's `runtime: "node24"` with the explicit `vercel/sandbox/universal:latest` image after the live runtime booted Amazon Linux 2023 despite the current SDK README describing Ubuntu. Universal is Ubuntu and still includes Node 24. Tradeoff: the image choice, rather than the runtime shortcut, is now pinned in snapshot construction.
- **[Recording]** WebReel 0.1.4's lazy ffmpeg URL is dead: its fixed 7.1 BtbN asset returns 404 because the rolling release now carries 8.1/9.0. The snapshot installs BtbN's static 8.1 GPL binary to `/usr/local/bin/ffmpeg` and runtime commands set `FFMPEG_PATH`, removing that network dependency. Tradeoff: the snapshot script owns a direct binary URL until WebReel fixes its installer.
- **[Recording]** A7 failed. WebReel's recording path ignores `CHROME_PATH` and unconditionally calls `ensureHeadlessShell()`; only visible preview mode honors the shared Chrome. The snapshot therefore contains agent-browser Chrome plus WebReel's headless shell. Avoiding the second ~190 MB browser would require forking WebReel, which is not justified for the take-home.
- **[Recording]** Patched the snapshotted WebReel 0.1.4 runtime in two version-checked places: remove Linux's `--enable-begin-frame-control` (its recorder never sends begin-frame CDP commands, so screenshots never resolve in Sandbox), and surface ffmpeg stderr/exit status instead of a later `rename ENOENT`. Tradeoff: snapshot construction intentionally fails when an upstream release changes either source string, forcing this workaround to be reviewed.
- **[Snapshot]** The model-authored smoke was initially too short and read the wrong output path: zero captured frames exposed the ffmpeg error, and WebReel writes relative output beside the config after its internal `videos/..` normalization. The final 3-second, two-step smoke produced a 27.7 KiB mp4 before snapshot creation.
- **[Sandbox]** Built immutable snapshot `snap_bXxesYYnAQIfU8iKWN78dIE7zFCs` in `iad1`, then stored it as a readable `SANDBOX_SNAPSHOT_ID` Config value in Production, Preview, and Development. A8 held with a 407 ms restore; record-only replay returned an 8.05s 1440×900 60fps video and poster in 18.9s (23.2s total including file transfer), leaving ample room under the five-minute limit.
- **[Build]** The Next.js build now creates the runner first and explicitly includes `.runner/runner.mjs` in output file tracing. Without both, local VM tests pass while deployed Workflow functions fail trying to read a git-ignored build artifact.

## Sandbox runner — checkpoint 4 (immutable Blob artifacts)

- **[Storage]** Kept all keys deterministic and rejected path separators, traversal components, control characters, invalid PR numbers, overflowing event sequences, and non-slug stages before they reach Blob. Event sequences are zero-padded to four digits so lexical listing preserves stage order.
- **[Storage]** Uploads are immutable and retry-safe: `head()` returns an existing object, `put()` uses `allowOverwrite: false` and no random suffix, and a second `head()` resolves the race where another retry wins between those calls. Tradeoff: a bad first write cannot be corrected in place; a rerun needs a new deployment/run identity, which matches the append-only model.
- **[Storage]** Added an `onConfig` handoff immediately after Sandbox extraction and before the record command. Video and poster upload in parallel only after recording, and terminal metadata writes last with all three artifact URLs. Live Blob headers show config at 18:06:57 UTC and video at 18:06:58 UTC, proving the required ordering rather than relying on code inspection.
- **[Storage]** The record-only checkpoint is publicly readable at the exact R-6.1 prefix for PR #1 and its real deployment: config, 323 KiB video, poster, then metadata. A second live run restored the snapshot in 489 ms and completed record + transfer in 28.8s.
- **[Failure]** A11 held inside the real snapshot: a wrong bypass failed as `explore/preview-protected` in 9.8s, before AI Gateway, then persisted credential-free logs and append-only event `runs/checkpoint-wrong-bypass/events/0000-explore.json` carrying `{ stage, reason, logsUrl }`.
- **[Dependencies]** Added only `@vercel/sandbox` and `@vercel/blob` for this slice; they are the fixed platform primitives in the spec, replacing no viable standard-library implementation.

## Trigger and orchestration — planning (docs/plan-trigger-and-orchestration.md)

- **[Trigger]** Verified the live deployment record before writing the payload schema: every `deployment.meta` value is a string (`githubPrId` is `"1"`), `deployment.url` is a bare host with no scheme, and repo identity is `githubOrg`/`githubRepo` (not the `githubCommit*` pair, which names the fork source). The plan's zod schema validates `githubPrId` as a digit string and converts it once, rather than `z.coerce.number()`.
- **[Trigger]** Moved the tag check from Workflow step 1 (spec §6) into the webhook handler. The handler already makes the one GitHub call that returns the title, so checking there means an untagged PR creates no Workflow run and no Blob object of any kind. Tradeoff: the handler does more inline work, all of it sub-second.
- **[Idempotency]** The deployment sentinel is claimed last, immediately before `start()`, using Blob's `allowOverwrite: false` as the only conditional-write primitive; the loser of a race sees `claimed: false`. Filters and the GitHub call run first so a transient GitHub failure never permanently burns a deployment. Confirmed `start()` in Workflow SDK 4.8.6 has no idempotency-key option of its own, so this is the mechanism rather than a belt-and-braces addition.
- **[Identity]** The Workflow run ID is the run ID; no separate UUID or mapping. Tradeoff: the `(repo, prNumber)` in-progress pointer can only be written after `start()`, leaving a sub-second window where a second deployment could double-start. Accepted; the deployment sentinel still forbids duplicate runs per deployment, and a 15-minute staleness guard on pointers prevents a stuck run from locking a PR out forever.
- **[Workflow]** Provision → explore → record → upload is one step with `maxRetries = 0` that returns `{ ok, failure }` instead of throwing, because Workflow retries cannot tell `provision/create-failed` (retry once, spec §8) from `explore/preview-protected` (never retry), and because Sandbox handles and mp4 buffers must not enter the serialized event log. The single provision retry moves inside `provisionSandbox()`.
- **[Scope]** The Workflow ships with a pure `fallbackDemoSpec(pr)` (tag-stripped title, derived slug, `entryPoint: null`) in place of the AI scoping call, which is blocked on Gateway billing. It emits the same `DemoSpec` type so the model step replaces one line later. Rejected an env toggle to force record-only from the webhook: live runs should fail honestly as `explore/model-call-failed` while the account is unfunded, which also exercises the failure comment for real.
- **[Storage]** Made every Blob listing explicitly sort by pathname instead of depending on service ordering, and verified the deployment sentinel against the live store: the same ID returned `claimed: true` then `claimed: false` before the throwaway object was deleted. Tradeoff: small list results are buffered before reads, bounded here by the run-event and per-PR prefixes.
- **[Workflow]** A1 held for orchestration but not for the plan's direct `tsx` start call: `next dev` runs the local Workflow world, while a standalone script imports an untransformed function and gets `start-invalid-workflow-function`. Added a development-only start route for the temporary harness; tradeoff: one extra route ships in this checkpoint and is deleted with the harness.
- **[Workflow]** A2 and A3 held in a real record-only run: plain `RecordDemoInput`, `DemoSpec`, changed paths, `Outcome`, artifact URLs, and timings crossed step boundaries without serialization or event-size errors. The six immutable events were `scope → comment → provision → record → upload → done`; steady local status reads were 171–368 ms after a 3.2 s cold compile.
- **[Security]** The wrong-bypass probe exposed that WebReel's `element-not-found` detail includes its resolved URL, so the first failure event briefly contained replay credentials. Deleted all seven public objects and the local Workflow records for that run, then centralized redaction at the Sandbox boundary and verified the replacement event/log contain `<redacted>` and none of the three secrets. Tradeoff: raw recorder diagnostics lose credential values, which must never be persisted anyway.
- **[Recording]** A wrong bypass in record-only mode fails as `record/element-not-found`, not `preview-protected`, because explicit auth-wall detection exists only in exploration. The redacted detail still names the failed step, URL shape, and selector, so the failure remains actionable.
- **[Trigger]** A6 and the pre-claim filter order held in local replay: bad signatures returned 401; other event types, production targets, non-allowlisted repos, and completed demos returned their specific 200 skip reasons; signing the exact fixture bytes verified `request.text()` without reserialization. Tradeoff: the fixture proves local byte handling, while the first production delivery still has to verify Vercel's envelope.
- **[GitHub]** Kept the GitHub integration as a typed REST `fetch` wrapper rather than adding Octokit; exact PR lookup, branch fallback, changed-file pagination, and comment upsert need only six endpoints. A7 held: `owner:branch` resolved PR #1 and reached `already-demoed`. Tradeoff: comment and file scans are deliberately capped at 300 records.
- **[Demo target]** Added and pushed `feat/low-stock-filter` as the second deterministic target feature, but the `ghp` token cannot open its PR because its refs are not readable (REST 422); did not switch to the unrelated default GitHub identity. Tradeoff: start/race/live-delivery assertions remain blocked until the token gains Contents read and `DEMO_LOGIN_TOKEN` is rotated.
- **[Model]** Re-probed AI Gateway after promotional credit increased the key's balance to $25: `anthropic/claude-sonnet-5` is allowlisted but still returns 403 because free-tier credit cannot fund that model, while all six zero-priced language models return the distinct team-restricted 403. Tradeoff: the explorer remains blocked until paid credit is added or a free model is enabled.
- **[Model]** A further $10 credit addition cleared the paid-model restriction: low-effort smoke calls to `openai/gpt-5.6-luna` and `openai/gpt-6-astra` both returned the exact requested text in 1.43 s and 1.12 s, consuming $0.0006296 total. Tradeoff: this proves Gateway access, not browser-tool reliability.
- **[Comment]** Recovery always searches the stable PR marker before creating when the remembered comment ID is absent or returns 404, so a lost response or manual deletion cannot produce two bot comments. Tradeoff: recovery scans at most 300 comments, matching the GitHub wrapper's deliberate cap.
- **[Testing]** Interpreted the comment plan's “no `!`” assertion as human-copy punctuation after excluding required HTML-comment (`<!--`) and Markdown-image (`![`) syntax; applying it literally makes the marker and poster requirements impossible to satisfy. Tradeoff: tests distinguish structural Markdown from visible prose.
- **[Closeout]** Kept `trigger-run.ts`, `replay-webhook.ts`, and the production-disabled dev trigger route until the blocked live assertions and rerun work are complete; deleting them now would remove the only repeatable verification path. The production build passed, unsigned webhooks returned 401, and the dev trigger returned 404 under `next start`. Tradeoff: temporary harness code remains visible for one more slice.
- **[Security]** Rotated the exposed demo-login token in local development and all three environments of both Vercel projects, then deployed both apps and verified the fresh target preview exchanged it for a session and reached `/` with 200. Tradeoff: immutable older target deployments retain the old environment snapshot and cannot be used for new-token replay.
- **[Deploy]** Deployed the completed trigger/comment pipeline to `preview-reel.vercel.app`; the production alias returns 401 for an unsigned webhook and 404 for the development-only trigger route. Tradeoff: end-to-end webhook and comment transitions still need PR #2's Git-backed deployment.
- **[Trigger]** A5 and A6 held on PR #2's real `deployment.succeeded`: production verified the signature/envelope, resolved the tagged PR, claimed and indexed one Workflow run, and a signed replay returned `already-claimed` with no second run. The failed run retained one credential-free event/log trail and updated comment ID `5608924861` in place. Tradeoff: the run exhausted exploration's 25-action budget before emitting a config, so successful recording and A10's concurrent-deployment check remain open.
- **[Exploration]** Added the first 10,000 characters of the PR body as delimited, explicitly untrusted explorer context because the target's discoverability instructions live there, and persisted the redacted action transcript before Sandbox teardown on both success and failure. Tradeoff: very long PR bodies are truncated and their content still consumes model context.
- **[Exploration]** The model found and exercised PR #2 correctly but repeatedly emitted agent-browser `press`/`fill` names instead of WebReel `key`/`type`, then discovered that a no-op click passed the broad union schema. Added exact replay examples and made the second invalid finish terminal. Tradeoff: schema repair gets one retry, as specified, rather than spending the action budget guessing.
- **[Validation]** Deleted the four no-op artifacts from the first completed PR #2 probe, retained its run transcript, and reran after documenting the complete WebReel step contract; the final 10-step config opens `Control+k`, filters `6202`, opens the matching drawer, records a 709 KiB video, and updates the same PR comment in 80.4s. Tradeoff: three empty target commits remain as an honest record of live deployment probes.
- **[Model]** Made `openai/gpt-5.6-luna` with medium reasoning the shared default for scoping and exploration after a live scoping call returned a valid command-palette spec in 4.9s. Tradeoff: medium effort spends more time and tokens than low effort, while one centralized constant keeps a future measured downgrade simple.
- **[Scope]** Replaced the title-only fallback with an AI Gateway JSON call over PR title/body, changed paths, and preview URL; Zod validates without coercion, one malformed response gets one repair call, and a second failure records `scope/invalid-output` with a failed PR comment. Tradeoff: failed scoping stores a run record with `demo: null` because inventing a fallback spec would violate the no-default rule.
- **[Privacy]** Disabled Gateway's ZDR requirement for Luna after the live call proved the allowlisted OpenAI route has no ZDR-attested credentials; accepted standard provider retention for this public-repo demo. Tradeoff: private-repo support must restore ZDR through attested BYOK or another allowed route before launch.

## UI and API — planning (docs/plan-ui-and-api.md)

- **[Storage]** Audit found that a manual re-run against the same deployment would hit `putOnce`'s existing `config.json`/`video.mp4` and return the *old* artifacts as if freshly written. Planned fix nests demo artifacts under `{deploymentId}/{runId}/` rather than enabling `allowOverwrite`, which would put a rewritten poster behind the 60s CDN cache R-6.4 warns about. Tradeoff: the R-6.1 key shape gains one segment; the gallery dedupes to the newest metadata per `(repo, prNumber)`.
- **[Storage]** `metadata.json` currently omits `runId`, `commitSha`, `logsUrl`, and model cost (R-6.2, R-10.2), so a gallery card cannot link to its run. Planned a shared zod `demoMetadataSchema` for writer and reader; the reader warns-and-skips objects that fail so one bad object cannot blank the gallery. Tradeoff: the two pre-schema objects already in Blob are not migrated by hand — a record-only re-run of PR #2 writes a conforming one.
- **[UI]** Kept the wireframe's search box as a client-side substring filter over server-rendered cards; the spec cuts *filter-by-feature* (a taxonomy), which this is not. Dropped the wireframe's "vercel project" header link and settings icon: there is no team slug or project URL in the fixed env list, and `04-settings.png` is out of scope per `wireframes/README.md`. Tradeoff: the header links to the target GitHub repo instead; per-run preview and PR links carry the real URLs.
- **[Rerun]** The re-run route will be public like the rest of the app (§11 auth punt) and will deliberately skip the completed-demo and deployment-sentinel guards — those stop *automatic* re-recording (R-2.10); this is the manual path. It keeps the in-progress guard so a PR never has two concurrent runs. Tradeoff: an unauthenticated full re-run spends model budget; the AI Gateway spend cap is the backstop.
- **[UI]** Server components read Blob directly; the only JSON the UI polls is the existing `GET /api/runs/[runId]`, extended with `demo`, `configUrl`, `logsUrl`, `transcriptUrl`. No `/api/demos`, no data-fetching library. Tradeoff: four extra `head()` calls per 3s poll tick, acceptable at demo scale (assumption A1 in the plan).
- **[Identity]** Split the plan's proposed run-bearing `DemoIdentity` into deployment-level `DemoIdentity` and `DemoArtifactIdentity { runId }`, because the webhook must construct `RecordDemoInput` before Workflow assigns its run ID. Tradeoff: artifact helpers have a narrower identity type while trigger input remains constructible.
- **[Cost]** A live Luna probe disproved the planned generation lookup: AI SDK's response ID is `aitxt-…`, the actual `gen_…` ID lives in `providerMetadata.gateway.generationId`, and `getGenerationInfo()` still failed with that ID after 4.25 seconds. Both scoping and exploration now read the immediately available decimal string `providerMetadata.gateway.cost`. Tradeoff: this records documented inference cost and excludes add-on charges such as reporting-tag writes; costs are never estimated.
- **[UI/API checkpoint 1]** Added run-scoped immutable artifact keys, strict typed demo metadata, newest-per-PR and recency helpers, five-phase status derivation, completed-demo listing, and config/transcript readers. Tradeoff: legacy Blob metadata is warn-and-skip until a re-run writes the new schema.
- **[Planning]** Reverted the in-progress guard extraction because its second caller (the rerun route) does not exist yet, violating the repo's no-speculative-abstraction rule; a dedicated Workflow guard module is deferred until that route lands. The initial diagnosis that the new `workflow/api` import caused the smoke script to hang was wrong: `npx tsx` remained delayed after the revert, while invoking the local `tsx` binary directly ran immediately. Tradeoff: the webhook keeps its local guard for one more checkpoint.
- **[Validation]** A live disposable Blob probe wrote run-scoped config/video/poster/metadata, read the same metadata by run, found it through the cross-demo listing, confirmed the config URL, and deleted all four objects; a prefix check found zero leftovers. It also confirmed legacy metadata is warned-and-skipped without hiding valid new records. Tradeoff: dummy media bytes test storage contracts, not video playback or range requests.
- **[Gallery]** Replaced poster cards with grouped border-wrapped tables because the gallery needs faster visual scanning across demos. Tradeoff: the table keeps the poster compact and omits a header row; the feature title and poster link to the run while the PR cell links directly to GitHub.
- **[Run status]** Derive terminal status from append-only events before consulting Workflow; local development also trusts in-progress events because a local Workflow runtime cannot resolve production run IDs and left the page request open for minutes. Workflow remains the fallback for eventless runs and deployed in-progress crash detection. Tradeoff: an unexpected local Workflow crash can leave a development-only run shown as in progress.
- **[Validation]** Live record-only runs verified failed, active, and completed UI states, client polling through completion, config rendering, poster/video range loading, and run-scoped metadata. Tradeoff: the first run intentionally retained its selector failure as a realistic failure-state fixture; the successful run updated the PR comment and became the newest demo.

## Inspection disclosures — planning (docs/plan-inspection-disclosures.md)

- **[Planning]** Wrote the steps/logs disclosure brief from the written requirements and the codebase; the Figma MCP quota ran out after one metadata call (only the "Design direction" readme frame was read), so the mockups were not inspected. The brief tells the implementer to pull them with `get_design_context` first and treats the brief as authoritative on behaviour, the mockup on layout. Tradeoff: layout details in the plan are structural, not pixel guidance.
- **[UI]** Steps / Raw JSON will be two `Button`s with WAI-ARIA tab roles rather than a generated shadcn `tabs` component, because the requirement names Collapsible, Skeleton, Alert, and Button as the primitive set and the switch is ~15 lines. Tradeoff: hand-rolled roving focus; fall back to `tabs` if it grows past 30 lines.
- **[UI]** Run logs render only `at` beside `data`; `stage`/`stream` are dropped and no severity, duration, or video-time is derived, because those facts belong to the phase list and failure alert and the log chunks are raw process output, not structured events. Tradeoff: stderr and stdout are visually indistinguishable in the disclosure.

## Inspection disclosures — build

- **[UI]** Pinned the app to shadcn's existing `.dark` token set by adding one class on `<html>`, because every Figma frame is dark and the readme frame calls the direction "Vercel-inspired". Chose a fixed theme over a toggle: a toggle is out of scope in `AGENTS.md`, and `prefers-color-scheme` would make preview screenshots depend on the reviewer's OS. Tradeoff: no light mode.
- **[UI]** Mapped Tailwind's `font-sans` token to Next's `--font-geist-sans` variable instead of the self-referential `--font-sans` value, so the default Geist face is actually applied. Tradeoff: the app now depends on the explicit Geist font variables defined by `next/font`.
- **[UI]** Matched the supplied Reel detail frames by making the run page a wider `max-w-5xl` shell, moving source metadata into the player card, and rendering active/failed phases as a two-column card. This keeps status, source, and pipeline state in the same hierarchy as the screenshots without adding a page-level design system. Tradeoff: the player card is intentionally desktop-oriented and stacks source details below the video on small screens.
- **[UI]** Step labels are a lookup over the twelve actions in `lib/scope/schema.ts::webreelStepSchema` with a verbatim fallback (`actionLabel` returns the raw action name in monospace for anything else), instead of validating steps with that schema. The schema constrains what the model may emit; the UI must render whatever the runner actually saved, including steps a future `webreel` accepts that the schema does not. Tradeoff: an unknown action gets a raw label, never an error.
- **[UI]** The expanded step body is `JSON.stringify(step, null, 2)` in a `<pre>`, not a key/value tree. It preserves key order, nesting, `${VAR}` placeholders, and output paths for free; a tree renderer would be more code than the rest of the feature. Tradeoff: nested steps such as `drag { from, to }` read as JSON rather than as rows.
- **[UI]** Steps / Raw JSON are two `Button`s with WAI-ARIA tab roles and arrow-key focus movement (about 25 lines) rather than a generated shadcn `tabs` component, keeping the primitive set to the four the brief named. Tradeoff: hand-rolled roving focus.
- **[Testing]** Skipped the unit tests the plan listed for `extractSteps`, `actionLabel`, and `primaryValue`; verification is on the Vercel preview against the Figma frames. Per the assessment brief, a legible component beats a test suite here. Tradeoff: a regression in the fallback path would surface in the UI, not CI.
- **[Validation]** Will not use the local app for this UI slice because a current Vercel/runtime issue prevents it from loading reliably; local triage is deferred. Tradeoff: each related UI commit is checked through its Vercel preview deployment and browser screenshots instead of local tests or a local dev server.
- **[UI]** Kept run-log parsing as a small line parser and rendered each saved chunk as one wrapped `<pre>` beside its UTC timestamp; stage, stream, severity, duration, and video-time interpretations are intentionally omitted because the persisted data is raw runner output. Tradeoff: stdout and stderr are visually indistinguishable.
- **[Preview]** Both UI slices passed the remote Vercel build and returned HTTP 200 through `vercel curl`; browser screenshots are blocked by the project's Deployment Protection redirect to Vercel Login. Tradeoff: visual interaction verification remains pending until preview access is granted or protection is triaged.

## Demo readiness — planning (docs/plan-demo-readiness.md)

- **[Planning]** With a day left, chose four items from the assessment rubric over polish: the rerun route (the on-stage "watch it run" moment and the promise the failed-state comment already makes), placeholders as a visible feature, an explicit auth-wall reason in the record path, and model/cost visibility. Cut stale-demo detection and model-on-failed-runs to a carry-forward list. Tradeoff: two existing reels will show "model not recorded" until re-run.
- **[Process]** Each slice ships as its own PR, validated on the Vercel preview build (API via `vercel curl`, UI on production after merge), and merged before the next branches from `main`. Tradeoff: no local dev-server checks (see `[Validation]`); a UI regression is seen on production, not before.

## Demo readiness — build

- **[Rerun]** Extracted the webhook's in-progress guard into `lib/runs/in-progress.ts` now that the rerun route is its second caller; it returns the active `runId` rather than a boolean so the route can point a 409 at the running run. It is the only module outside `app/` and `workflows/` that imports `workflow/api`, and it stays out of `lib/storage/runs.ts`, which standalone scripts import. Tradeoff: two entry points now share one 15-minute freshness window.
- **[Rerun]** A record-only re-run reuses the *source run's* `previewUrl` and `pr` snapshot rather than refetching the PR: identity is `(repo, prNumber)` and the title is display material, so a re-run after a title edit records the old title. Tradeoff: stale title on the re-run's comment until the next automatic run.
- **[Rerun]** Order of checks is 404 → 400 → 409 in-progress → 409 no-config → start, and the index write happening *after* `start()` is reported as `202 { runId }` even if the pointer write fails (logged as `run-index-failed`): the run is already going, and hiding it behind a 5xx would invite a second click. Tradeoff: a failed pointer write weakens the in-progress guard for that PR for one run.
- **[Validation]** On the slice A preview, `POST …/rerun` returned 400 / 404 / 409 `no-config` / 202 / 409 `run-in-progress` exactly per contract, and the re-run updated PR #2's comment in place (comment id `5608924861` before and after). The run itself failed at `record`, and the cause was env drift, not code: `DEMO_LOGIN_TOKEN` had been rotated on 09-09 by updating the shared entry, but a Preview-only entry from 09-08 was never updated and shadowed it in Preview. Deleted the stale entry (`6LX523vLqhMQbwMI`); Production was never affected. Tradeoff: one wasted run (`wrun_01M25XP74AW07H94WFA8782NDW`) with a misleading `navigation-failed` reason — exactly the gap slice E closes.
- **[Auth wall]** Probed the target with curl before writing slice E: a wrong bypass secret gets `302 → https://vercel.com/sso-api?…` (A2 holds); a wrong login token gets a **401 from `/api/demo-login`** after Vercel's cookie hop, not a redirect to `/login` (A3 is false). The good path is `307` (Vercel strips the bypass params and sets `_vercel_jwt`) → `302 /` → `200`, so the preflight must carry cookies across hops or the second hop lands on the SSO wall and misreports `preview-protected`.
- **[UI]** Re-run controls live once, in the run page's header action cluster next to "Visit preview", on `done` and `failed` runs; they are not duplicated under the failure alert, which stays diagnostic. "Re-run recording" is disabled with a visible line of copy (not only a tooltip) when the run produced no config. The client is `useState` + `fetch` + `router.push`; a 409 `run-in-progress` renders inline with an "Open it" link, everything else renders the response detail inline. Tradeoff: on a failed run the eye lands on the alert first and has to travel up to act.
- **[Storage]** `metadata.json` gains `model: { id, reasoningEffort } | null`, written from the shared constants in `lib/ai/model.ts` (the one place a model change shows up) and read with `.nullable().default(null)` so the objects written before the field still list in the gallery and render "not recorded". This is storage-schema evolution, not model-output coercion; `AGENTS.md`'s "never coerce or default" stays absolute for zod on model output. A test pins that `.default` does not loosen `.strict()` for unknown keys (assumption A6 held). Tradeoff: reels recorded before this commit show "not recorded" until re-run.
- **[UI]** Provider icon by id prefix, one provider, no registry: `openai/…` gets an inline OpenAI mark (`fill-current`, `aria-hidden`), anything else renders the name alone; display names are a one-entry lookup with the id suffix as fallback. Write the second provider before extracting a registry. Tradeoff: a non-OpenAI model renders without a mark.
- **[Gallery]** The spend line under the subtitle sums reported cost over the reels shown (newest per PR) and says so — "N reels · $x in model spend for these reels" — and drops the spend clause when nothing was reported. Not lifetime spend, not a per-row column; the table's right edge belongs to the timestamp. `formatCost`/`formatDuration` moved to `lib/format.ts` now that the gallery is their second caller. Tradeoff: superseded runs' spend is not counted anywhere in the UI.
- **[UI]** The Steps tab now shows each video's entry URL (resolved against `baseUrl` when relative) with every `${VAR}` token rendered as a `<mark>`, plus one caption stating that credentials are substituted at replay time and never written to the file; the same splitter runs over step values and the expanded JSON. This makes the rule `containsResolvedCredential` enforces legible instead of incidental. Tradeoff: the highlight is a regex over `${UPPER_SNAKE}` — a lowercase placeholder would render as plain text.
- **[Runner]** Auth-wall detection in the record path is a preflight, not output parsing: before `webreel record`, the entry URL is `${VAR}`-substituted in memory and its redirect chain followed manually (≤5 hops, 10 s each, a minimal cookie jar so Vercel's `_vercel_jwt` from the 307 reaches the next hop). Classification is a pure function with a unit test pinned to the observed hops. Webreel's own exit text remains the fallback classifier. A preflight network error or timeout is logged and the run continues — the preflight sharpens a reason, it must not add a new way to fail. Tradeoff: one extra round trip (~200 ms) per recording.
- **[Runner]** Because A3 was false (a bad token gets a 401 from `/api/demo-login`, not a redirect to `/login`), the rule set is: 3xx to `vercel.com` → `preview-protected`; 401/403 whose request path is `/api/demo-login` → `login-failed`; any other 401/403 → `preview-protected`; same-origin 3xx to `/login` → `login-failed` (kept for the explore path's symmetry); everything else proceeds. Tradeoff: the login route path is a constant in the runner, matching the target app `AGENTS.md` specifies.
- **[Copy]** Failure detail for both paths is now the spec's wording — "Preview deployment is protected — check the bypass secret." and "Target app rejected the demo login token — check DEMO_LOGIN_TOKEN." — with no URL, status, or `Location` interpolated, because the preflight URL holds resolved secrets. Tradeoff: the detail no longer says which hop tripped; the run log's step output still does, redacted.
- **[Process]** Kept the dev trigger harness (`app/api/dev/trigger-run`, `scripts/trigger-run.ts`) one more round: it is how the failure fixtures are staged with an overridden secret. Delete it in the README close-out.

## Connection model

- **[Onboarding]** One team-level Vercel webhook plus an env allowlist, instead of per-repo installation. Tradeoff: target projects must live in the same Vercel team; a real product needs a Vercel Integration.
- **[Trigger]** Resolve the PR from the branch via the GitHub API rather than trusting git metadata in the webhook payload. Tradeoff: one GitHub call on every allowlisted preview deployment, so the untagged path is cheap rather than free.
- **[Auth]** Fine-grained PAT over a GitHub App. Tradeoff: single-repo, single-tenant, manual rotation.
- **[Access]** Protection Bypass for Automation to reach protected preview deployments. Tradeoff: the bypass secret grants access to every preview on that project.
- **[Webhook]** Ack within 30s and hand off to the Workflow, because Vercel aborts at 30s and retries non-2xx for up to 24 hours. Delivery order is not guaranteed, so idempotency is a hard requirement rather than a nicety.
- **[Links]** Chose an explicit `APP_BASE_URL` over Vercel's injected production-host variable because the latter was unavailable inside a Workflow step and caused PR comments to link to localhost. Tradeoff: the production alias must be configured in each environment.