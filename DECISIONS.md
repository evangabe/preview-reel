# DECISIONS.md

Running log of choices, tradeoffs, and assumptions. One line each. Appended as work happens, not reconstructed afterwards.

Format: `- **[Area]** Chose X over Y because Z. Tradeoff: W.`

---

## Pre-build (scoping)

- **[Problem]** Chose preview-deployment demo recording over a general demo generator because the trigger, the environment, and the audience are all already defined by the PR. Tradeoff: only useful to teams already on Vercel preview deployments.
- **[Build vs buy]** Chose to build on `webreel` (Vercel Labs, Apache-2.0) rather than implement frame capture and encoding. Tradeoff: the project reads as a layer on someone else's tool unless the authoring and trigger work is substantial.
- **[Architecture]** Split exploration from recording: `agent-browser` emits a `webreel` config, `webreel` replays it. Because an agent's live fumbling is unpublishable and a config is reviewable and re-runnable. Tradeoff: two stages, two failure modes, longer runs.
- **[Trigger]** Chose a `[feat]` PR title tag over a `CHANGELOG.md` diff. Deterministic, zero-cost on the common path, and puts the decision with the author. Tradeoff: only fires when someone remembers to tag.
- **[Trigger]** Chose title tags over GitHub labels because labels need repo setup. Tradeoff: pollutes PR titles, and a tag added after the fact only fires on the next deployment.
- **[Identity]** Keyed runs on `(repo, prNumber)` rather than feature slug or deployment ID. PR numbers are stable; titles get edited and deployments repeat per push.
- **[Model]** The model scopes the demo but does not decide whether to record. Removes the false-positive problem entirely rather than tuning for it.
- **[Comment]** One comment updated in place, never the PR description. A bot that overwrites human-authored text loses that fight every time.
- **[Storage]** Blob metadata instead of a database. Tradeoff: cross-run queries for the gallery get awkward; escape hatch is a per-repo index blob, not Postgres.
- **[Scope]** Cut: credential management UI, TTS voiceover, uploaded audio, stale-demo detection, multiple demos per PR, gallery auth. Each is a real feature; none is needed to prove the loop.
- **[Scope]** Hardcoded credentials for v1. The correct answer is a scoped demo account with short-lived credentials via Vercel Connect, which is a day of work and not the interesting part.

## Assumptions to verify in the first hour

- **[Sandbox]** Assuming `webreel`'s auto-download of Chrome and ffmpeg to `~/.webreel` works inside a Sandbox microVM, or can be pinned via `CHROME_PATH` / `FFMPEG_PATH` in a snapshot. Unverified — try pointing `webreel` at the Chromium `agent-browser install` already places in the snapshot before letting it fetch its own.
- **[Trigger]** ~~Assuming the deployment webhook payload carries enough git metadata to resolve the PR title without a GitHub API call.~~ Checked against Vercel's webhook API docs before writing code: it doesn't — `deployment.meta` carries git identifiers (`githubPrId`, `githubCommitRef`, `githubCommitSha`), not title/body. A GitHub call is still required either way; the win is that `githubPrId` (when present) turns it from a branch search into an exact `GET` by PR number.
- **[Agent]** Assuming `agent-browser`'s accessibility snapshot yields selectors stable enough to replay. Unverified — may need a resolution pass between explore and record.
- **[Timing]** Assuming end-to-end wall clock under 4 minutes. If it's longer, the in-progress state needs more design than a stage label.

## Spec review, pre-build (docs-only pass against current Vercel/webreel/agent-browser docs)

- **[Bug]** Spec originally said filter webhooks on `target: "preview"`. Vercel's actual values are `production`, `staging`, or `null` — preview deployments are `null`. That filter would have silently dropped every event the project exists to process. Caught by reading the webhook API reference before writing the handler, not by testing it live. Fixed to `target !== "production"`.
- **[Credentials]** Spec assumed the bypass secret and login could be injected as HTTP headers/cookies into the recorded flow. `webreel`'s config schema has no header or cookie field — only URL, steps, and viewport per video. Chose URL query params with `webreel`'s built-in `${VAR}` env-substitution over forking `webreel` or writing a custom replay layer. Tradeoff: the bypass secret and session token are visible in the browser's address bar in the recorded video (mitigated: it's a single-use redirect that immediately drops the query string via the target app's own redirect to a clean URL) and the target app needs one new route (`/api/demo-login`) it wouldn't otherwise have.
- **[Auth]** Cut a scripted login step in favor of a server-side token-for-cookie exchange (`/api/demo-login`), resolving what was Open Question 2 by design rather than by testing whether session-state injection works. Removed `TARGET_APP_EMAIL`/`TARGET_APP_PASSWORD` from the env list; replaced with a single `DEMO_LOGIN_TOKEN`.
- **[Storage]** Spec's status page assumed polling a single Blob object that gets overwritten per stage. Vercel Blob's `cacheControlMaxAge` floor is 60s and overwrites take up to 60s to propagate through the CDN — a live-updating status page built on that would show a stale stage for most of a 3-minute run, directly undermining R-8.2. Changed run-status records to append-only per-stage-transition objects (or reading `getRun()` from the Workflow directly), keeping Blob as the durable terminal record rather than the live one.
- **[Scope]** Cut gallery filter-by-feature (R-8.1) and per-repo daily run cap (R-10.3) during the review pass, not mid-build. Neither is needed to prove the loop; both were added to the original spec without being load-bearing for the demo. The AI Gateway spend cap remains the actual cost backstop.
- **[Idempotency]** Spec's idempotency requirement (R-1.5) only covered duplicate webhook deliveries for the same deployment ID. It didn't cover a second deployment for a PR that already has a run *in progress* (a push mid-run) — §8 says "let the current run finish," but nothing enforced that before this pass. Added an in-progress check keyed on `(repo, prNumber)`, separate from R-2.10's completed-record check.
- **[Timing]** §12's build order summed to 420 minutes against a 360-minute budget — a plan that's already over budget before the first line of code, with no acknowledgment of where to cut. Left the per-step estimates alone (they're the right shape) but named the cut explicitly: drop to the record-only re-trigger fallback at hour 4 rather than let step 1 run long.

## Build

<!-- append below as you go -->

- **[Repo]** GitHub repo name is `preview-reel` (matches AGENTS.md's repository table), not the local folder name `vercel-preview-reels`. Created public under the `evangabe` account via the `ghp` gh-cli alias, not the default `gh` identity, which is signed in as a separate work account.
- **[Scaffold]** Ran `create-next-app` in a scratch dir and merged the output in, rather than scaffolding in place, because the target directory already had `AGENTS.md`/`DECISIONS.md`/`docs/`/`wireframes/` and `create-next-app` refuses a non-empty target.
- **[Scaffold]** Local shell aliases `npx`/`npm` through a Socket Firewall proxy (`sfw`) that intercepts TLS for package-fetch scanning. `shadcn init`'s runtime call to `ui.shadcn.com/init` failed under that proxy (`unable to get local issuer certificate`) but succeeded identically with `SFW_BYPASS=1`. Used the bypass for `shadcn init` and `npm install`/`npm run build`; not a project-level config change, just how this one shell reaches those hosts.
- **[Scaffold]** Populated `lib/`, `sandbox-runner/`, `workflows/`, `scripts/`, and the `app/api`/`app/runs` routes as empty `export {}` stubs with a comment pointing at the spec section, since AGENTS.md's repository layout names these files explicitly. Left `components/{gallery-grid,run-status,demo-player,empty-state}.tsx` out for now — AGENTS.md says "do not build the gallery before the pipeline produces a video," and stub files for those read as starting that work rather than just laying out structure.
- **[Scaffold]** Added a `type-check` script but not a `test` script — no test runner is installed and no pure functions exist yet to test. Adding `vitest` now with nothing to run would leave `npm test` broken, against "keep it running." Lands with the first pure function that needs a test (tag matcher is first up, build order step 2).
- **[Scaffold]** `.gitignore`'s default `.env*` rule from `create-next-app` would have excluded `.env.example` too; added `!.env.example` so the template stays committed while real `.env*` files stay ignored.
- **[Tooling]** The Cursor agent harness auto-appends `Co-authored-by: Cursor <cursoragent@cursor.com>` to every commit made through its shell tool, regardless of the `-m` message given — confirmed by amending with the trailer stripped and watching it get re-added. Not caused by any git hook, alias, or config in this repo or globally (checked `.git/hooks`, `core.hooksPath`, `~/.gitconfig`, env for `GIT_*`/`LD_PRELOAD`/`DYLD_*` — none present), so it's injected above the shell, not something `git commit` itself can be told to skip. `git commit-tree`/`filter-branch` don't go through that path and aren't intercepted, so history was rewritten with `filter-branch --msg-filter` and force-pushed once, and subsequent commits in this session are made via `write-tree`+`commit-tree`+`update-ref` instead of `git commit`.

## Vercel project setup

- **[Plan]** Team webhooks are Pro/Enterprise only (`webhook_triggers` is "not available" on Hobby), so the plan is a hard prerequisite rather than a nice-to-have — R-1.1 is the entire trigger. Confirmed the team is on `pro` via the API before building on it. Everything else in the stack (Sandbox, Blob, Workflow, AI Gateway, Protection Bypass) works on Hobby; this one feature forces the upgrade. Worth stating in the README so a reviewer cloning this doesn't get halfway.
- **[Config]** `VERCEL_OIDC_TOKEN` is **not** a runtime environment variable in deployed functions. The spec and `.env.example` both listed it alongside the hand-set secrets, and validating it at boot 500'd every dynamic route in production while `/` (static, CDN-served) kept returning 200 — a failure that only shows up on deploy, not in `npm run build`. It exists at build time and in `.env.local` via `vercel env pull` (12h expiry); at runtime the Sandbox SDK acquires it out of band. Now validated off-platform only, gated on `process.env.VERCEL`. This was my assumption, carried straight from the spec, and the deploy is what caught it.
- **[Config]** Split `.env.example` into hand-set secrets vs. platform-injected ones, and gave `lib/env.ts` a different remedy string per group. "Add a variable" and "connect a Blob store" are different actions, and telling someone to paste an OIDC token into the dashboard points them at a value that expires in 12 hours. Confirmed empirically: connecting the Blob store injected `BLOB_READ_WRITE_TOKEN` automatically.
- **[Config]** Validate env in `instrumentation.ts` (runtime boot) rather than at build. R-CFG.1 says "fail the deploy", but the webhook secret cannot exist before the first deploy — a build-time check makes the first deploy structurally impossible. Tradeoff: a missing secret 500s every dynamic route instead of failing the build, which is loud but later than the spec wanted.
- **[Config]** Seeded `VERCEL_WEBHOOK_SECRET`/`GITHUB_TOKEN`/`VERCEL_PROTECTION_BYPASS` with obvious placeholders for the first deploy, then replaced the webhook secret with the real value after registration. Tradeoff: presence-only validation passes on a placeholder, so the check proves configuration exists, not that it works.
- **[Config]** Hand-rolled the env validator instead of pulling in zod. Six required strings don't need a schema library, and zod lands with `lib/scope/schema.ts`, which is the file that actually needs it.
- **[Region]** Pinned `iad1` in `vercel.json`. The Vercel World backend for Workflow is deployed only in `iad1`; apps elsewhere route workflow requests there anyway and pay the latency. The Blob store also landed in `iad1`, so storage, orchestration, and functions are colocated.
- **[Deploy]** `vercel project add` creates a project with no framework preset, so the first deploy failed with `The Output Directory "public" is empty`. Fixed with `"framework": "nextjs"` in `vercel.json` rather than clicking it in the dashboard, so the setting is committed and reviewable.
- **[Workflow]** Did not set `maxDuration` anywhere. `withWorkflow()` already emits step functions at `maxDuration: max` and orchestration at 60s, so R-3.3's 5-minute Sandbox budget is covered by the plan maximum on Pro. Setting it by hand would have been a guess at a value the integration already manages.
- **[Storage]** Created the Blob store with `--access public`. R-6.3 accepts public-read via unguessable URL, and the PR comment embeds a poster that GitHub's image proxy fetches unauthenticated. Tradeoff: the v1 privacy punt is now a property of the store, not just the code.
- **[Trigger]** Registered the team webhook unscoped (all team projects) per R-1.1, and made the unimplemented handler return 200 rather than 501. Vercel retries any non-2xx with backoff for 24 hours, so a stub that honestly reports "not implemented" would have generated a day of retries per deployment. Verified live: a deploy fired `deployment.succeeded` and the endpoint answered 200.
- **[Access]** Preview Reel's own production alias (`preview-reel.vercel.app`) is public, while deployment-specific URLs sit behind Vercel Authentication and 302 to a login. The webhook must target the alias — pointing it at a deployment URL would auth-wall Vercel's own delivery service. Same protection asymmetry the target project will rely on in reverse.

## Step 0b: live verification against the target (before any pipeline code)

- **[Trigger]** Confirmed against the target project's real deployments: the PR preview arrives with `target: null` and `meta.githubPrId: 1`. R-1.3's `target !== "production"` filter and R-1.8's `githubPrId` fast path are now verified, not assumed.
- **[Auth]** Verified the PAT end to end on PR #1 — create 201, update 200, delete 204 — with Issues and Pull requests both at read/write. Not tested with the spec's narrower split, so the widening is defensive rather than proven necessary; it stays because a 403 at the comment stage is the worst place to discover the difference.
- **[Access]** The auth wall has a fingerprint: a wrong or missing bypass secret 302s to `vercel.com/sso-api?url=…&nonce=…`, while a correct secret against an unauthenticated app 302s to the same-origin `/login`. R-3.6's "detect the auth wall explicitly" is therefore a host check on the redirect target — `vercel.com/sso-api` means the bypass is wrong, same-origin `/login` means the demo-login step didn't run. Two different failure reasons, cheaply distinguishable.
- **[Access]** The query-param form (`?x-vercel-protection-bypass=…&x-vercel-set-bypass-cookie=true`) answers 307 to the clean URL and sets `_vercel_jwt` (7-day, HttpOnly). So `webreel`'s first navigation carries the secret exactly once and every subsequent step in the recording is cookie-authenticated with a clean address bar — the mitigation in the earlier `[Credentials]` entry holds.
- **[Auth]** Resolved spec Open Question 2 by running it: a fresh cookie-less context hitting `/api/demo-login?token=…&next=/` plus the bypass params lands on `/` with 200 after two redirects, where `/` otherwise 302s to `/login`. A wrong token yields 401 at `/api/demo-login`. Login failure and auth-wall failure are distinct status codes on distinct hosts.
- **[Recording]** Inspected `webreel@0.1.4` and `@webreel/core` rather than trusting the docs: Chrome launches with `--no-sandbox` already (so running as the Sandbox user is fine), `CHROME_PATH`/`FFMPEG_PATH` are honored, binaries cache under `~/.webreel/bin/`, and `${VAR}` substitution leaves unset variables as literal text — so a missing env var at replay time produces an "Element not found"/navigation failure, not a clear error. The runner must assert the substitution variables are present before invoking `webreel record`.
- **[Recording]** `webreel`'s step vocabulary is `pause, click, key, type, drag, scroll, wait, moveTo, screenshot, navigate, hover, select`; targets are `text` or `selector` with optional `within`. There is no header, cookie, or auth field of any kind, and `webreel validate` exists — the explore stage should run it on the emitted config before declaring success.

## Sandbox runner — checkpoint 1 (hand-driven golden path)

- **[Exploration]** A1 held, with a CLI caveat: `agent-browser` followed the demo-login redirects and retained both bypass/session cookies, but `open --headers …` followed by commands without that launch option restarted Chrome at `about:blank`. `set headers` once before `open` preserved the daemon session across commands. The explorer will use the persistent setting, not repeat `--headers` on only its first command.
- **[Selectors]** The target already gives its replay-critical controls stable test IDs (`select-P-1001`, `select-P-1002`, `export-csv`), so the golden config uses CSS selectors for checkboxes and visible text for `Export CSV`. Tradeoff: IDs tied to fixture part IDs are target-specific, which is appropriate for the fallback config but not something the model prompt should generalize.
- **[Recording]** A2 held: `webreel` 0.1.4 replayed the six-step config, including `click.text = "Export CSV"`, against a fresh context and produced a valid 1440×900 60fps mp4 plus thumbnail. The deterministic fallback is therefore viable before any model code exists.
- **[Recording]** A3 was based on the wrong visual model: WebReel captures only the page viewport, not Chrome's address bar, so the secret cannot appear in a video frame even during the initial redirect. The query-param secret can still appear in infrastructure request logs; the clean redirect remains security-relevant, but `thumbnail.time` is not its mitigation.
- **[Fixtures]** Chose the second warmed recording (10.05s, 409 KiB) as the checked-in fixture over the first cold-install run (2.73s). The first run downloaded Chrome/ffmpeg while recording and produced compressed timing that did not match the configured delays; snapshot builds must smoke once after installation for both correctness and warm caches.
- **[Validation]** Mirrored WebReel's action union in zod but deliberately narrowed model-produced navigation to same-origin paths, typing to stable selectors, one target per action, one video, and ≤12 steps. Tradeoff: valid hand-authored WebReel configs may fail Preview Reel's stricter schema; this boundary validates what the model is allowed to emit, not every config WebReel can execute.
- **[Testing]** Added Vitest with the first zod schema, per the repository rule to test pure schemas only. Used Vitest 4 rather than 5 because the local Node 25 binary is outside Vitest 5's explicit engine range, while both support the deployed Node 24 runtime.
- **[Dependencies]** Added AI SDK + AI Gateway, zod, esbuild, and tsx now because the next vertical slice uses each directly: model tool loop, validated config, single-file VM bundle, and local runner harness. No wrapper packages or provider SDKs were added.

## Sandbox runner — checkpoint 2 (local stage contracts)

- **[Model]** Chose `anthropic/claude-sonnet-5` from the live AI Gateway model list for the exploration reliability probe, prioritizing tool-use quality over the cheaper mini models while the key's $25 monthly budget is the hard cap. Tradeoff: the three-run score costs more; if it passes, model downgrades are a later measured optimization.
- **[Model]** The first explorer call reached AI Gateway only with the local Socket Firewall bypass; without it, the proxy's certificate produces `unable to get local issuer certificate`. With the bypass, Gateway returned a definitive 403: the Pro team still requires a valid credit card on file even though the API key has a budget. Classified this as `explore/model-call-failed` rather than the earlier misleading `invalid-config`; the 2-of-3 score is blocked on account setup, not browser behavior.
- **[Exploration]** The model returns only `{ found, reason, steps }`; runner code constructs the auth URL, base URL, viewport, output, and thumbnail. This narrows the plan's `finish(config)` contract because letting the model repeat fixed credential placeholders creates risk without adding judgment.
- **[Privacy]** Explorer transcripts log every browser tool call and model step but redact all three credentials before writing. Verified the failed live run's transcript contains none of `DEMO_LOGIN_TOKEN`, `VERCEL_PROTECTION_BYPASS`, or `AI_GATEWAY_API_KEY`.
- **[Runner]** Kept the required ESM single-file bundle and added a `createRequire(import.meta.url)` banner because the AI Gateway package's optional OIDC path contains a dynamic CommonJS `require("path")`; plain esbuild ESM crashed before command dispatch. Tradeoff: a one-line compatibility shim in the build command, instead of changing the runner contract to CommonJS.
- **[Recording]** The record stage rewrites only the output/thumbnail fields into its own output directory, removes stale outputs, validates before launch, captures child stderr for one-line failure JSON, and streams WebReel stdout. The bundled 931 KiB runner reproduced the golden config in 15.6s (411 KiB mp4); with both substitution variables removed it exited 1 with no stdout and one `record/substitution-vars-missing` JSON line.
- **[Failure]** Added `record-failed` and `explore/model-call-failed` as honest fallback reasons beyond the plan's initial closed taxonomy. Mapping an unknown recorder failure to `navigation-failed` or a Gateway/account failure to `invalid-config` would be specific-sounding but false.

## Sandbox runner — checkpoint 3 (snapshot and VM replay)

- **[Sandbox]** Replaced the plan's `runtime: "node24"` with the explicit `vercel/sandbox/universal:latest` image after the live runtime booted Amazon Linux 2023 despite the current SDK README describing Ubuntu. Universal is Ubuntu and still includes Node 24. Tradeoff: the image choice, rather than the runtime shortcut, is now pinned in snapshot construction.
- **[Recording]** WebReel 0.1.4's lazy ffmpeg URL is dead: its fixed 7.1 BtbN asset returns 404 because the rolling release now carries 8.1/9.0. The snapshot installs BtbN's static 8.1 GPL binary to `/usr/local/bin/ffmpeg` and runtime commands set `FFMPEG_PATH`, removing that network dependency. Tradeoff: the snapshot script owns a direct binary URL until WebReel fixes its installer.
- **[Recording]** A7 failed. WebReel's recording path ignores `CHROME_PATH` and unconditionally calls `ensureHeadlessShell()`; only visible preview mode honors the shared Chrome. The snapshot therefore contains agent-browser Chrome plus WebReel's headless shell. Avoiding the second ~190 MB browser would require forking WebReel, which is not justified for the take-home.
- **[Recording]** Patched the snapshotted WebReel 0.1.4 runtime in two version-checked places: remove Linux's `--enable-begin-frame-control` (its recorder never sends begin-frame CDP commands, so screenshots never resolve in Sandbox), and surface ffmpeg stderr/exit status instead of a later `rename ENOENT`. Tradeoff: snapshot construction intentionally fails when an upstream release changes either source string, forcing this workaround to be reviewed.
- **[Snapshot]** The model-authored smoke was initially too short and read the wrong output path: zero captured frames exposed the ffmpeg error, and WebReel writes relative output beside the config after its internal `videos/..` normalization. The final 3-second, two-step smoke produced a 27.7 KiB mp4 before snapshot creation.
- **[Sandbox]** Built immutable snapshot `snap_bXxesYYnAQIfU8iKWN78dIE7zFCs` in `iad1`, then stored it as a readable `SANDBOX_SNAPSHOT_ID` Config value in Production, Preview, and Development. A8 held with a 407 ms restore; record-only replay returned an 8.05s 1440×900 60fps video and poster in 18.9s (23.2s total including file transfer), leaving ample room under the five-minute limit.
- **[Build]** The Next.js build now creates the runner first and explicitly includes `.runner/runner.mjs` in output file tracing. Without both, local VM tests pass while deployed Workflow functions fail trying to read a git-ignored build artifact.

## Connection model

- **[Onboarding]** One team-level Vercel webhook plus an env allowlist, instead of per-repo installation. Tradeoff: target projects must live in the same Vercel team; a real product needs a Vercel Integration.
- **[Trigger]** Resolve the PR from the branch via the GitHub API rather than trusting git metadata in the webhook payload. Tradeoff: one GitHub call on every allowlisted preview deployment, so the untagged path is cheap rather than free.
- **[Auth]** Fine-grained PAT over a GitHub App. Tradeoff: single-repo, single-tenant, manual rotation.
- **[Access]** Protection Bypass for Automation to reach protected preview deployments. Tradeoff: the bypass secret grants access to every preview on that project.
- **[Webhook]** Ack within 30s and hand off to the Workflow, because Vercel aborts at 30s and retries non-2xx for up to 24 hours. Delivery order is not guaranteed, so idempotency is a hard requirement rather than a nicety.