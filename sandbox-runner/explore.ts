// agent-browser drives the (bypass-header-authenticated) preview URL and
// emits a webreel config — never a video (R-4.1, R-4.2). ≤12 steps
// (R-4.3), stable role/test-id selectors (R-4.4), hard action budget
// (R-4.5). Config persisted whether or not recording succeeds (R-4.6).
// Not implemented.
export {};
